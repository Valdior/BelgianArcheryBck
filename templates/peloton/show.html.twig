{% extends 'base.html.twig' %}

{% block title %}Peloton{% endblock %}

{% block body %}
    <h1>Peloton</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ peloton.id }}</td>
            </tr>
            <tr>
                <th>MaxParticipant</th>
                <td>{{ peloton.maxParticipant }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ peloton.type }}</td>
            </tr>
            <tr>
                <th>StartTime</th>
                <td>{{ peloton.startTime ? peloton.startTime|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('peloton_index') }}">back to list</a>

    <a href="{{ path('peloton_edit', {'id': peloton.id}) }}">edit</a>

    {{ include('peloton/_delete_form.html.twig') }}
{% endblock %}
