{% extends 'base.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
    <h1>Edit Participant</h1>

    {{ include('participant/_form.html.twig') }}

    <a href="{{ path('peloton_show', {'tournament': tournament.id, 'id': participant.peloton.id}) }}">back to list</a>
{% endblock %}